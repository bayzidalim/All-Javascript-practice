var depth = 5;
if (depth<=10) {
    animal = depth * 50;
}
else if (depth <= 20) {
    var firstPart = depth * 50;
    var remaining = depth - 10;
    var secondPart = remaining * 100;
    animal = firstPart + secondPart;
}
else if (depth <= 30) {
    var 
}